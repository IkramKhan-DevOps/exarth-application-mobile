import 'package:flutter/material.dart';

class LogInMid extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Container(
      child: 
      Column(children: [
        Text("EXARTH",style: TextStyle(
          fontWeight: FontWeight.bold,
          fontSize: 40,
          
        ),),
      Text("Your Technology Partner")
      ],)
    );
  }
}